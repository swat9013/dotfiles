# コンテキストアーキテクチャ リファレンス

## TOC
- [4層コンテキスト構造の全体俯瞰](#4層コンテキスト構造の全体俯瞰)
- [各層の読み込みタイミングとコスト](#各層の読み込みタイミングとコスト)
- [コンテンツ配置の判断基準](#コンテンツ配置の判断基準)
- [6段階最適化ワークフロー](#6段階最適化ワークフロー)
- [/compact vs /clear の使い分け](#compact-vs-clear-の使い分け)
- [MCP との連携](#mcp-との連携)
- [トークン節約チェックリスト](#トークン節約チェックリスト)

---

## 4層コンテキスト構造の全体俯瞰

```
読み込みタイミング: 早い ←────────────────────────→ 遅い
コンテキスト消費:   常時 ←────────────────────────→ オンデマンド

CLAUDE.md → Rules → Skills ──────────────── Subagents
  ↓          ↓        ↓                        ↓
 全体指針   パス条件  自動判断 or /slash実行   コンテキスト分離
```

| 観点 | CLAUDE.md | Rules | Skills | Subagents |
|------|-----------|-------|--------|-----------|
| 読み込み | セッション開始時 | パス条件マッチ時 | Claude判断 or `/skill` | Task tool 経由 |
| コンテキスト | 常時（全文） | 条件付き | 概要のみ事前、詳細は必要時 | 分離（トークン消費なし） |
| 用途 | プロジェクト全体の指針 | パス固有のルール | ワークフロー・手順 | 並列実行・専門タスク |
| 推奨サイズ | 150行以下 | 200行以下 | 500行以下 | - |

**[2025年変更]** Slash Commands（`.claude/commands/`）は Skills（`.claude/skills/`）に統合済み。既存の `.claude/commands/` は後方互換性を維持。

---

## 各層の読み込みタイミングとコスト

```
CLAUDE.md（常時ロード）      : 高コスト（全文常時）
rules/（パスマッチ時）        : 中コスト（条件付き）
skills/ descriptions のみ    : 低コスト（~100 tokens/スキル）
skills/ 本文（呼び出し時）    : 中コスト（<5k tokens）
skills/ references/（必要時） : ほぼゼロ（読み込まれるまでトークン消費なし）
```

**情報の普遍性と配置**:
```
高  │  CLAUDE.md（全体原則）
    │      ↓
    │  Rules（パス条件付き詳細）
    │      ↓
    │  Skills（ワークフロー・手順）
    │      ↓
低  │  Subagents（分離実行タスク）
    └────────────────────────────→ コンテキスト消費（低 → 高[分離]）
```

---

## コンテンツ配置の判断基準

### 抽出先決定テーブル

| 内容の種類 | 抽出先 | 読み込みタイミング |
|-----------|--------|------------------|
| ファイル拡張子・ディレクトリ規約 | `.claude/rules/{topic}.md` | パスアクセス時 |
| 3ステップ以上のワークフロー | `.claude/skills/{name}/SKILL.md` | Claude判断 or `/skill` |
| 副作用なし、参照情報のみ | `.claude/skills/` (`user-invocable: false`) | Claude自動判断 |
| 上記に該当しない必須情報 | **CLAUDE.mdに残す** | セッション開始時 |

### 決定フロー

```
「このルールは特定のパスにのみ適用される？」
├─ YES → Rules（paths指定）
└─ NO →「複数ステップのワークフロー？」
    ├─ YES → Skills
    └─ NO → CLAUDE.mdに残す
```

### CLAUDE.md に残すべき内容

- プロジェクト概要（1-2文）
- 基本コマンド（build, test, lint）
- すべての操作に必須のルール

---

## 6段階最適化ワークフロー

```
1. 分析 → 2. 分類 → 3. 計画 → 4. 抽出 → 5. リファクタ → 6. 報告
```

1. **分析**: CLAUDE.md の行数とセクション構造を確認
2. **分類**: 抽出先決定テーブルで各セクションの抽出先を決定
3. **計画**: 抽出計画をユーザーに提示し承認を得る
4. **抽出**: 適切なフロントマターでファイル作成
5. **リファクタ**: CLAUDE.md を圧縮（50行未満が理想）
6. **報告**: before/after 行数と作成ファイルを表示

**効果の実例**: Kent Beck の 77 行 CLAUDE.md → **1 行**に圧縮（91%削減）

```markdown
# 圧縮後
Follow Kent Beck's Test-Driven Development methodology (tdd skill) as the preferred approach for all development work.
```

---

## /compact vs /clear の使い分け

| コマンド | 動作 | 使うタイミング |
|---------|------|--------------|
| `/compact` | 会話履歴をサマリーに圧縮 | 同じ作業を続けながらコンテキストを削減（使用率 70% 目安） |
| `/clear` | 会話履歴を完全リセット | 全く別タスクへ切り替える時、コミット完了後 |
| `/btw` | クイック質問（コンテキスト非消費） | 作業中の関連しない軽い質問 |

---

## MCP との連携

### Skills vs MCP

| 観点 | Skills | MCP |
|------|--------|-----|
| 提供するもの | 知識・手順（How） | ツール・データソース（What） |
| 方向性 | 指示 → Claude | 外部システム → Claude |
| 例 | クエリパターンの指導 | データベース接続 |

### 連携パターン

MCP がデータソース接続を提供し、Skill がその使い方（クエリパターン等）を指導する役割分担。

### MCP スコープ（2025年名称変更）

| 旧称 | 新称 | 保存先 | 用途 |
|------|------|--------|------|
| `global` | `user` | `~/.claude/mcp_servers.json` | ユーザー全体 |
| `project` | `local` | `.claude/mcp_servers.json` | プロジェクト固有・非共有 |
| (新) | `project` | `.mcp.json` | プロジェクト固有・バージョン管理対象 |

> 旧称 `project` は新称 `local` に変更。バージョン管理共有用の新スコープ `project`（`.mcp.json`）が追加。

### ENABLE_TOOL_SEARCH

MCP サーバーが多数存在する場合、ツール説明がコンテキストウィンドウの 10% を超えると自動的に有効化:

```bash
ENABLE_TOOL_SEARCH=auto    # デフォルト（10%超で自動有効）
ENABLE_TOOL_SEARCH=auto:5  # カスタム閾値5%
ENABLE_TOOL_SEARCH=true    # 常に有効
ENABLE_TOOL_SEARCH=false   # 無効
```

---

## ハーネス最適化原則

### ツールミニマリズム

ツール数の削減はパフォーマンスを向上させる。Vercelはエージェントのツール80%を削除し、ステップ数・トークン消費・速度が全て改善した。

- サブエージェントのツールを必要最小限に制限する（具体的な制限方法は subagent-orchestration.md の frontmatter `tools` を参照）
- MCP サーバーの過剰インストールを避ける（意思決定の麻痺を誘発）
- ツール説明がコンテキストウィンドウの5-7%を消費する点を意識する

### CLI vs MCP 判断基準

| 条件 | 推奨 | 理由 |
|------|------|------|
| CLIが訓練データに含まれる（git, docker, npm等） | CLI（Bash経由） | 高い正確性 + grep/jq との合成可能性 |
| 社内API・新規サービス | MCP サーバー | 訓練データ外のため明示的なツール定義が必要 |

CLI ラッパースクリプト（6-8個の使用例付き）は、フルスペックの MCP サーバーよりトークン効率が高い。

### 段階的ハーネス簡素化

モデルアップグレード時には、1つのスカフォールディング層を削除して影響を測定する。前モデルで必要だった制約が新モデルでは不要な場合がある（例: 新モデルでコンテキスト追従性が改善し、冗長なリマインダーが不要に）。ハーネスの各構成要素が「本当に耐荷重か」を定期的に検証する。

### 状態管理のフォーマット選択

| 形式 | 用途 | リスク |
|------|------|--------|
| JSON | 機械可読な状態追跡（feature list, session state） | 手動編集時の構文エラー |
| Markdown | 人間向けドキュメント、CLAUDE.md | エージェントによるドリフト（意図しない書き換え） |

長期実行エージェントの進捗追跡には JSON を推奨。Markdown は構造が崩れやすい。

---

## トークン節約チェックリスト

- [ ] CLAUDE.md は 150 行以下か
- [ ] パス固有のルールは Rules に分離したか
- [ ] ワークフローは Skills に分離したか
- [ ] Skills の詳細は `references/` に分離したか
- [ ] 並列実行可能なタスクは Subagents を使用しているか
- [ ] コンテキスト使用率 70% 到達前に `/compact` を検討したか
