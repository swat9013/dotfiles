---
paths: .zsh/keybinds.zsh
---

# Zshキーバインド設計

## キーバインド一覧

| キー | 機能 | 説明 |
|------|------|------|
| Ctrl+r | fzf頻度順履歴検索 | コマンド履歴を使用頻度順に表示・検索 |
| Ctrl+s | fzfディレクトリナビゲーション | ディレクトリをインタラクティブに選択・移動 |
| Ctrl+^ | 一つ上のディレクトリへ | `cd ..` の短縮 |
| Ctrl+m | 入力なし→ls+git status | 空行でEnter時に自動実行 |
| Ctrl+v | VSCode起動 | カレントディレクトリをVSCodeで開く |

## fzf統合

**fzf**: 汎用コマンドラインファジーファインダー

### Ctrl+r (頻度順履歴検索)

コマンド履歴を使用頻度順（最頻使用が先頭）で表示し、fzfでフィルタリング。選択したコマンドをコマンドラインに挿入。

- `--no-sort`: 入力順序（頻度順）を保持
- `--scheme=history`: 履歴向けマッチング（コマンド先頭の一致を重視）

### Ctrl+s (ディレクトリナビゲーション)

カレントディレクトリ配下のディレクトリを検索し、選択したディレクトリに移動。

## 設計方針

- **インタラクティブ選択**: キーボード操作で効率的に選択
- **Unix哲学**: 小さく単機能なツールの組み合わせ
