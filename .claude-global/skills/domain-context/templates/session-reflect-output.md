# セッション振り返り結果

## セッション情報

- **ファイル**: `{session_file_path}`
- **メッセージ数**: {user_messages}件（ユーザー）/ {assistant_messages}件（アシスタント）
- **分析日時**: {analysis_timestamp}

## 抽出結果サマリー

| # | カテゴリ | 内容 | 保存先 | 確信度 |
|---|----------|------|--------|--------|
| 1 | {category} | {summary} | {destination} | {confidence} |

**凡例**:
- **カテゴリ**: アーキテクチャ / 技術選択 / コーディング規約 / 落とし穴 / コマンド
- **保存先**: CLAUDE.md / docs/{filename}
- **確信度**: High / Medium / Low

## 詳細

### 1. {title}

**カテゴリ**: {category}
**確信度**: {confidence}

**内容**:
{detailed_description}

**根拠**（セッションからの引用）:
```
{quote_from_session}
```

**推奨保存先**: {destination}
**推奨セクション**: {section_in_file}

---

## 判断基準

以下の基準で抽出しました:

- ✅ **具体的で再利用可能**: このプロジェクト固有の学び
- ✅ **文脈依存**: セッション内で発見・確認された事実
- ❌ **汎用的**: 一般的なベストプラクティスは除外
- ❌ **一時的**: セッション限りの作業メモは除外

## 次のステップ

### 既存ドキュメントとの整合性チェック

以下を確認してください:

- [ ] 既存 CLAUDE.md との重複はないか
- [ ] 既存 docs/ の内容と矛盾しないか
- [ ] 同じトピックを扱う既存セクションに統合すべきか

### 追記実行

確認後、以下のコマンドで追記できます:

```
追記を実行しますか？ [y/N]
```

または個別に編集:

- `{destination}` の `{section}` セクションに追記
